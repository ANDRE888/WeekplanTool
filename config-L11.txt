# ============================================================
#  Test-BoxCount-L11 - configuratie / конфигурация (ЛИНИЯ 11)
#  Пути к файлам с данными. Строки с # - комментарии.
#  Формат: ключ = значение
#  ВАЖНО: это ОТДЕЛЬНЫЙ файл. config.txt рядом - для линии 9,
#  его этот скрипт не трогает.
# ============================================================

# Путь к файлу сбора коробок (фактическая продукция):
BoxPrintingFile = C:\Users\andre\Documents\WeekplanTool\Data_boxprintingbin3v7.xlsb

# Папка с файлами недельного плана (daily shift NDwkNN-YYYY.xls):
PlanFolder = C:\Users\andre\Documents\WeekplanTool

# ---- ЛИНИЯ 11: ДВА листа сырых коробок и ДВА листа истории ----
# У линии 11 два доспринтера (Pr1 и Pr2), поэтому листов по два.
# Скрипт читает оба и СКЛАДЫВАЕТ - ровно так же, как это делает SAPSTATus
# (сумма совпала 1:1 с сеткой день/смена на листе "W <неделя>").
# Разделитель - запятая.
BoxSheet  = Boxruw111,Boxruw112
RcdbSheet = L11P1RCDB,L11P2RCDB

# ---- прочие настройки (те же, что у линии 9) ----
# Явный файл плана (иначе берётся свежий daily shift NDwk*.xls* из PlanFolder):
# PlanFile = C:\...\daily shift NDwk36-2026.xls
# Резервный target на смену, если дня нет в плане (или задан -NoPlan):
# ShiftTarget = 1234
# Окно для "темпа последних минут" (мин):
# RecentMinutes = 30
# Разрыв между коробками ОДНОЙ МАШИНЫ, который считается простоем (мин).
# На линии 11 такт машины ~20-25 с, так что 2 мин - разумный порог:
# StopMinutes = 2
# Раздел "История": 0 = текущая производственная неделя (с воскресенья, как в SAPSTATus),
# N > 0 = скользящее окно в N дней, -1 = раздел выключен:
# HistoryDays = 0
