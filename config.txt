# ============================================================
#  Test-BoxCount-Lines - configuratie / конфигурация
#  ОДИН файл на ВСЕ линии (5, 6, 8, 9, 11). Скрипт читает его при старте.
#
#  Формат:    ключ = значение        Строки с # - комментарии. Кодировка UTF-8.
#  Блоки:     всё, что стоит ДО первого [N], действует на ВСЕ линии;
#             строка вида [6] открывает блок линии 6, и ключи в нём перекрывают
#             общий блок ТОЛЬКО для этой линии.
#  Приоритет: параметр командной строки > блок [N] > общий блок > реестр в скрипте.
#
#  Раньше конфигов было пять (config.txt, config-L5/L6/L8/L11.txt) - теперь всё здесь.
# ============================================================


# ############################################################
#  ОБЩЕЕ ДЛЯ ВСЕХ ЛИНИЙ
# ############################################################

# ---- ДВА ПУТИ, КОТОРЫЕ НАДО ЗАПОЛНИТЬ ----
# Все линии читают ОДИН и тот же файл сбора коробок и ОДНУ папку планов,
# поэтому эти ключи стоят здесь, а не в блоках линий.
#
#   BoxPrintingFile - файл сбора коробок (фактическая продукция), *.xlsb
#   PlanFolder      - папка с недельными планами (daily shift NDwkNN-YYYY.xls)
#
# --- ПРОДАКШЕН (на заводе): раскомментировать эти две строки, а тестовые ниже закрыть # ---
# BoxPrintingFile = F:\users\DATA_COLLECTIE\boxprinting\Data_boxprintingbin3v7.xlsb
# PlanFolder      = F:\users\Planning\FORECAST\
#
# --- ТЕСТ (эта машина): сейчас активно ---
BoxPrintingFile = C:\Users\andre\Documents\WeekplanTool\Data_boxprintingbin3v7.xlsb
PlanFolder      = C:\Users\andre\Documents\WeekplanTool

# ---- ПРОЧИЕ НАСТРОЙКИ (значения по умолчанию для всех линий) ----
# Любую из них можно переопределить для одной линии в её блоке ниже.
#
# Явный файл плана (иначе берётся самый свежий daily shift NDwk*.xls* из PlanFolder):
# PlanFile = C:\Users\andre\Documents\WeekplanTool\daily shift NDwk37-2026.xls
# Резервный target на смену, если дня нет в плане (или задан -NoPlan):
# ShiftTarget = 1234
# Окно для "темпа последних минут" (мин):
# RecentMinutes = 30
# Разрыв между коробками ОДНОЙ МАШИНЫ, который считается простоем (мин):
# StopMinutes = 2
# Раздел "История": 0 = текущая производственная неделя (с воскресенья, как в SAPSTATus),
# N > 0 = скользящее окно в N дней, -1 = раздел выключен:
# HistoryDays = 0


# ############################################################
#  БЛОКИ ЛИНИЙ - только то, что у линии СВОЁ
#
#  Листы коробок и листы истории скрипт и так знает (реестр $script:LineDefs),
#  поэтому все ключи ниже закомментированы: блоки нужны лишь тогда, когда у линии
#  должна быть своя настройка (например свой ShiftTarget) или когда лист переименовали.
# ############################################################

# ---- ЛИНИЯ 5: несколько вкусов одновременно (машины 500...505 в теге 8) ----
[5]
# BoxSheet    = Boxruw5
# RcdbSheet   = L5RCDB
# ShiftTarget = 1234

# ---- ЛИНИЯ 6: несколько вкусов одновременно (машины 600...604 в теге 8) ----
[6]
# BoxSheet    = Boxruw6
# RcdbSheet   = L6RCDB
# ShiftTarget = 1234

# ---- ЛИНИЯ 8: несколько вкусов одновременно (машины 801...806 в теге 8) ----
[8]
# BoxSheet    = Boxruw8
# RcdbSheet   = L8RCDB
# ShiftTarget = 1234

# ---- ЛИНИЯ 9: ОДНА упаковочная машина (901), один вкус за раз ----
[9]
# BoxSheet    = Boxruw9
# RcdbSheet   = L9RCDB
# ShiftTarget = 1234

# ---- ЛИНИЯ 11: ДВА листа коробок и ДВА листа истории ----
# У линии 11 два доспринтера (Pr1 и Pr2), поэтому листов по два. Скрипт читает оба
# и СКЛАДЫВАЕТ - ровно так же, как это делает SAPSTATus (сумма совпала 1:1 с сеткой
# день/смена на листе "W <неделя>"). Разделитель - запятая. Машины 1101...1108.
[11]
# BoxSheet    = Boxruw111,Boxruw112
# RcdbSheet   = L11P1RCDB,L11P2RCDB
# ShiftTarget = 1234
