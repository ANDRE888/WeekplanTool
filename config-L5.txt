# ============================================================
#  Test-BoxCount-Lines - configuratie ЛИНИИ 5
#  Формат: ключ = значение. Строки с # - комментарии.
#  ВАЖНО: это файл ТОЛЬКО для линии 5. У каждой линии свой конфиг:
#  config.txt (9), config-L5.txt, config-L6.txt, config-L8.txt, config-L11.txt.
#  Один и тот же скрипт Test-BoxCount-Lines.ps1 читает их все при старте.
# ============================================================

# Путь к файлу сбора коробок (фактическая продукция).
# Ключ общий для всех линий - достаточно заполнить его в одном конфиге:
BoxPrintingFile = C:\Users\andre\Documents\WeekplanTool\Data_boxprintingbin3v7.xlsb

# Папка с файлами недельного плана (daily shift NDwkNN-YYYY.xls):
PlanFolder = C:\Users\andre\Documents\WeekplanTool

# ---- ЛИНИЯ 5: несколько вкусов одновременно ----
# Как и на линии 11, здесь параллельно работают несколько упаковочных машин,
# каждая со своим вкусом. Машина берётся из тега 8 этикетки (на этой линии
# встречались 500, 501, 503, 504, 505); сколько их - скрипт определяет сам, перечислять не нужно.
# Лист сырых регистраций коробок и лист 31-дневной истории:
BoxSheet  = Boxruw5
RcdbSheet = L5RCDB

# ---- прочие настройки (те же, что у остальных линий) ----
# Явный файл плана (иначе берётся свежий daily shift NDwk*.xls* из PlanFolder):
# PlanFile = C:\Users\andre\Documents\WeekplanTool\daily shift NDwk37-2026.xls
# Резервный target на смену, если дня нет в плане (или задан -NoPlan):
# ShiftTarget = 1234
# Окно для "темпа последних минут" (мин):
# RecentMinutes = 30
# Разрыв между коробками ОДНОЙ МАШИНЫ, который считается простоем (мин):
# StopMinutes = 2
# Раздел "История": 0 = текущая производственная неделя (с воскресенья, как в SAPSTATus),
# N > 0 = скользящее окно в N дней, -1 = раздел выключен:
# HistoryDays = 0
